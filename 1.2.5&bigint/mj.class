public class mj extends rt {
   private float a;

   public mj(xd par1World, double par2, double par4, double par6, double par8, double par10, double par12) {
      super(par1World, par2, par4, par6, par8, par10, par12);
      this.r = this.r * (double)0.01F + par8;
      this.s = this.s * (double)0.01F + par10;
      this.t = this.t * (double)0.01F + par12;
      double var10000 = par2 + (double)((this.U.nextFloat() - this.U.nextFloat()) * 0.05F);
      var10000 = par4 + (double)((this.U.nextFloat() - this.U.nextFloat()) * 0.05F);
      var10000 = par6 + (double)((this.U.nextFloat() - this.U.nextFloat()) * 0.05F);
      this.a = this.am;
      this.ao = this.ap = this.aq = 1.0F;
      this.e = (int)((double)8.0F / (Math.random() * 0.8 + 0.2)) + 4;
      this.S = true;
      this.c(48);
   }

   public void renderParticle(adz par1Tessellator, double par2, double par3, double par4, double par5, double par6, double par7) {
      float f = (float)(((double)this.d + par2) / (double)this.e);
      this.am = this.a * (1.0F - f * f * 0.5F);
      super.renderParticle(par1Tessellator, par2, par3, par4, par5, par6, par7);
   }

   public int b(float par1) {
      float f = ((float)this.d + par1) / (float)this.e;
      if (f < 0.0F) {
         f = 0.0F;
      }

      if (f > 1.0F) {
         f = 1.0F;
      }

      int i = super.b(par1);
      int j = i & 255;
      int k = i >> 16 & 255;
      j += (int)(f * 15.0F * 16.0F);
      if (j > 240) {
         j = 240;
      }

      return j | k << 16;
   }

   public float a(float par1) {
      float f = ((float)this.d + par1) / (float)this.e;
      if (f < 0.0F) {
         f = 0.0F;
      }

      if (f > 1.0F) {
         f = 1.0F;
      }

      float f1 = super.a(par1);
      return f1 * f + (1.0F - f);
   }

   public void J_() {
      this.l = this.o;
      this.m = this.p;
      this.n = this.q;
      if (this.d++ >= this.e) {
         this.A();
      }

      this.b(this.r, this.s, this.t);
      this.r *= (double)0.96F;
      this.s *= (double)0.96F;
      this.t *= (double)0.96F;
      if (this.z) {
         this.r *= (double)0.7F;
         this.t *= (double)0.7F;
      }

   }
}
