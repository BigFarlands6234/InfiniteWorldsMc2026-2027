public class rt extends nn {
   private int a;
   protected float b;
   protected float c;
   protected int d = 0;
   protected int e = 0;
   protected float am;
   protected float an;
   protected float ao;
   protected float ap;
   protected float aq;
   public static double ar;
   public static double as;
   public static double at;

   public rt(xd par1World, double par2, double par4, double par6, double par8, double par10, double par12) {
      super(par1World);
      this.a(0.2F, 0.2F);
      this.H = this.J / 2.0F;
      this.d(par2, par4, par6);
      this.ao = this.ap = this.aq = 1.0F;
      this.r = par8 + (double)((float)(Math.random() * (double)2.0F - (double)1.0F) * 0.4F);
      this.s = par10 + (double)((float)(Math.random() * (double)2.0F - (double)1.0F) * 0.4F);
      this.t = par12 + (double)((float)(Math.random() * (double)2.0F - (double)1.0F) * 0.4F);
      float f = (float)(Math.random() + Math.random() + (double)1.0F) * 0.15F;
      float f1 = gk.a(this.r * this.r + this.s * this.s + this.t * this.t);
      this.r = this.r / (double)f1 * (double)f * (double)0.4F;
      this.s = this.s / (double)f1 * (double)f * (double)0.4F + (double)0.1F;
      this.t = this.t / (double)f1 * (double)f * (double)0.4F;
      this.b = this.U.nextFloat() * 3.0F;
      this.c = this.U.nextFloat() * 3.0F;
      this.am = (this.U.nextFloat() * 0.5F + 0.5F) * 2.0F;
      this.e = (int)(4.0F / (this.U.nextFloat() * 0.9F + 0.1F));
      this.d = 0;
   }

   public rt c(float par1) {
      this.r *= (double)par1;
      this.s = (this.s - (double)0.1F) * (double)par1 + (double)0.1F;
      this.t *= (double)par1;
      return this;
   }

   public rt d(float par1) {
      this.a(0.2F * par1, 0.2F * par1);
      this.am *= par1;
      return this;
   }

   public void b(float par1, float par2, float par3) {
      this.ao = par1;
      this.ap = par2;
      this.aq = par3;
   }

   public float o() {
      return this.ao;
   }

   public float p() {
      return this.ap;
   }

   public float r() {
      return this.aq;
   }

   protected boolean e_() {
      return false;
   }

   protected void b() {
   }

   public void J_() {
      this.l = this.o;
      this.m = this.p;
      this.n = this.q;
      if (this.d++ >= this.e) {
         this.A();
      }

      this.s -= 0.04 * (double)this.an;
      this.b(this.r, this.s, this.t);
      this.r *= (double)0.98F;
      this.s *= (double)0.98F;
      this.t *= (double)0.98F;
      if (this.z) {
         this.r *= (double)0.7F;
         this.t *= (double)0.7F;
      }

   }

   public void renderParticle(adz par1Tessellator, double par2, double par3, double par4, double par5, double par6, double par7) {
      float f = (float)(this.a % 16) / 16.0F;
      float f1 = f + 0.0624375F;
      float f2 = (float)(this.a / 16) / 16.0F;
      float f3 = f2 + 0.0624375F;
      float f4 = 0.1F * this.am;
      double f5 = this.l + (this.o - this.l) * par2 - ar;
      double f6 = this.m + (this.p - this.m) * par2 - as;
      double f7 = this.n + (this.q - this.n) * par2 - at;
      float f8 = 1.0F;
      par1Tessellator.a(this.ao * f8, this.ap * f8, this.aq * f8);
      par1Tessellator.a(f5 - par3 * (double)f4 - par6 * (double)f4, f6 - par4 * (double)f4, f7 - par5 * (double)f4 - par7 * (double)f4, (double)f1, (double)f3);
      par1Tessellator.a(f5 - par3 * (double)f4 + par6 * (double)f4, f6 + par4 * (double)f4, f7 - par5 * (double)f4 + par7 * (double)f4, (double)f1, (double)f2);
      par1Tessellator.a(f5 + par3 * (double)f4 + par6 * (double)f4, f6 + par4 * (double)f4, f7 + par5 * (double)f4 + par7 * (double)f4, (double)f, (double)f2);
      par1Tessellator.a(f5 + par3 * (double)f4 - par6 * (double)f4, f6 - par4 * (double)f4, f7 + par5 * (double)f4 - par7 * (double)f4, (double)f, (double)f3);
   }

   public int n() {
      return 0;
   }

   public void b(ady nbttagcompound) {
   }

   public void a(ady nbttagcompound) {
   }

   public void c(int par1) {
      this.a = par1;
   }

   public int s() {
      return this.a;
   }

   public boolean k_() {
      return false;
   }
}
