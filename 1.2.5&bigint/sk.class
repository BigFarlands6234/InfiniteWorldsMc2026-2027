public class sk extends rt {
   float a;

   public sk(xd par1World, double par2, double par4, double par6, double par8, double par10, double par12) {
      this(par1World, par2, par4, par6, par8, par10, par12, 1.0F);
   }

   public sk(xd par1World, double par2, double par4, double par6, double par8, double par10, double par12, float par14) {
      super(par1World, par2, par4, par6, (double)0.0F, (double)0.0F, (double)0.0F);
      this.r *= (double)0.1F;
      this.s *= (double)0.1F;
      this.t *= (double)0.1F;
      this.r += par8 * 0.4;
      this.s += par10 * 0.4;
      this.t += par12 * 0.4;
      this.ao = this.ap = this.aq = (float)(Math.random() * (double)0.3F + (double)0.6F);
      this.am *= 0.75F;
      this.am *= par14;
      this.a = this.am;
      this.e = (int)((double)6.0F / (Math.random() * 0.8 + 0.6));
      this.e = (int)((float)this.e * par14);
      this.S = false;
      this.c(65);
      this.J_();
   }

   public void renderParticle(adz par1Tessellator, double par2, double par3, double par4, double par5, double par6, double par7) {
      float f = (float)(((double)this.d + par2) / (double)this.e) * 32.0F;
      if (f < 0.0F) {
         f = 0.0F;
      }

      if (f > 1.0F) {
         f = 1.0F;
      }

      this.am = this.a * f;
      super.renderParticle(par1Tessellator, par2, par3, par4, par5, par6, par7);
   }

   public void J_() {
      this.l = this.o;
      this.m = this.p;
      this.n = this.q;
      if (this.d++ >= this.e) {
         this.A();
      }

      this.b(this.r, this.s, this.t);
      this.ap = (float)((double)this.ap * 0.96);
      this.aq = (float)((double)this.aq * 0.9);
      this.r *= (double)0.7F;
      this.s *= (double)0.7F;
      this.t *= (double)0.7F;
      this.s -= (double)0.02F;
      if (this.z) {
         this.r *= (double)0.7F;
         this.t *= (double)0.7F;
      }

   }
}
