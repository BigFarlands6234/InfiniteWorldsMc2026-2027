import java.util.ArrayList;
import java.util.List;
import java.util.Random;

public class vo extends pt {
   private boolean a = false;
   private static List b = new ArrayList();

   public int a(int par1, int par2) {
      return par1 == 1 ? pb.av.a(par1, par2) : super.a(par1, par2);
   }

   private boolean checkForBurnout(xd par1World, long par2, int par3, long par4, boolean par5) {
      if (par5) {
         b.add(new alm(par2, par3, par4, par1World.w()));
      }

      int i = 0;

      for(int j = 0; j < b.size(); ++j) {
         alm redstoneupdateinfo = (alm)b.get(j);
         if (redstoneupdateinfo.a == par2 && redstoneupdateinfo.b == par3 && redstoneupdateinfo.c == par4) {
            ++i;
            if (i >= 8) {
               return true;
            }
         }
      }

      return false;
   }

   protected vo(int par1, int par2, boolean par3) {
      super(par1, par2);
      this.a = par3;
      this.a(true);
   }

   public int e() {
      return 2;
   }

   public void onBlockAdded(xd par1World, long par2, int par3, long par4) {
      if (par1World.getBlockMetadata(par2, par3, par4) == 0) {
         super.onBlockAdded(par1World, par2, par3, par4);
      }

      if (this.a) {
         par1World.notifyBlocksOfNeighborChange(par2, par3 - 1, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3 + 1, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2 - 1L, par3, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2 + 1L, par3, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3, par4 - 1L, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3, par4 + 1L, this.bO);
      }

   }

   public void onBlockRemoval(xd par1World, long par2, int par3, long par4) {
      if (this.a) {
         par1World.notifyBlocksOfNeighborChange(par2, par3 - 1, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3 + 1, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2 - 1L, par3, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2 + 1L, par3, par4, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3, par4 - 1L, this.bO);
         par1World.notifyBlocksOfNeighborChange(par2, par3, par4 + 1L, this.bO);
      }

   }

   public boolean isPoweringTo(ali par1IBlockAccess, long par2, int par3, long par4, int par5) {
      if (!this.a) {
         return false;
      } else {
         int i = par1IBlockAccess.getBlockMetadata(par2, par3, par4);
         if (i == 5 && par5 == 1) {
            return false;
         } else if (i == 3 && par5 == 3) {
            return false;
         } else if (i == 4 && par5 == 2) {
            return false;
         } else if (i == 1 && par5 == 5) {
            return false;
         } else {
            return i != 2 || par5 != 4;
         }
      }
   }

   private boolean isIndirectlyPowered(xd par1World, long par2, int par3, long par4) {
      int i = par1World.getBlockMetadata(par2, par3, par4);
      if (i == 5 && par1World.isBlockIndirectlyProvidingPowerTo(par2, par3 - 1, par4, 0)) {
         return true;
      } else if (i == 3 && par1World.isBlockIndirectlyProvidingPowerTo(par2, par3, par4 - 1L, 2)) {
         return true;
      } else if (i == 4 && par1World.isBlockIndirectlyProvidingPowerTo(par2, par3, par4 + 1L, 3)) {
         return true;
      } else if (i == 1 && par1World.isBlockIndirectlyProvidingPowerTo(par2 - 1L, par3, par4, 4)) {
         return true;
      } else {
         return i == 2 && par1World.isBlockIndirectlyProvidingPowerTo(par2 + 1L, par3, par4, 5);
      }
   }

   public void updateTick(xd par1World, long par2, int par3, long par4, Random par5Random) {
      boolean flag = this.isIndirectlyPowered(par1World, par2, par3, par4);

      while(b.size() > 0 && par1World.w() - ((alm)b.get(0)).d > 60L) {
         b.remove(0);
      }

      if (this.a) {
         if (flag) {
            par1World.setBlockAndMetadataWithNotify(par2, par3, par4, pb.aP.bO, par1World.getBlockMetadata(par2, par3, par4));
            if (this.checkForBurnout(par1World, par2, par3, par4, true)) {
               par1World.a((double)par2 + (double)0.5F, (double)par3 + (double)0.5F, (double)par4 + (double)0.5F, "random.fizz", 0.5F, 2.6F + (par1World.r.nextFloat() - par1World.r.nextFloat()) * 0.8F);

               for(int i = 0; i < 5; ++i) {
                  double d = (double)par2 + par5Random.nextDouble() * 0.6 + 0.2;
                  double d1 = (double)par3 + par5Random.nextDouble() * 0.6 + 0.2;
                  double d2 = (double)par4 + par5Random.nextDouble() * 0.6 + 0.2;
                  par1World.a("smoke", d, d1, d2, (double)0.0F, (double)0.0F, (double)0.0F);
               }
            }
         }
      } else if (!flag && !this.checkForBurnout(par1World, par2, par3, par4, false)) {
         par1World.setBlockAndMetadataWithNotify(par2, par3, par4, pb.aQ.bO, par1World.getBlockMetadata(par2, par3, par4));
      }

   }

   public void onNeighborBlockChange(xd par1World, long par2, int par3, long par4, int par5) {
      super.onNeighborBlockChange(par1World, par2, par3, par4, par5);
      par1World.scheduleBlockUpdate(par2, par3, par4, this.bO, this.e());
   }

   public boolean isIndirectlyPoweringTo(xd par1World, long par2, int par3, long par4, int par5) {
      return par5 == 0 ? this.isPoweringTo(par1World, par2, par3, par4, par5) : false;
   }

   public int a(int par1, Random par2Random, int par3) {
      return pb.aQ.bO;
   }

   public boolean g() {
      return true;
   }

   public void randomDisplayTick(xd par1World, long par2, int par3, long par4, Random par5Random) {
      if (this.a) {
         int i = par1World.getBlockMetadata(par2, par3, par4);
         double d = (double)par2 + (double)0.5F + (double)(par5Random.nextFloat() - 0.5F) * 0.2;
         double d1 = (double)par3 + 0.7 + (double)(par5Random.nextFloat() - 0.5F) * 0.2;
         double d2 = (double)par4 + (double)0.5F + (double)(par5Random.nextFloat() - 0.5F) * 0.2;
         double d3 = (double)0.22F;
         double d4 = (double)0.27F;
         if (i == 1) {
            par1World.a("reddust", d - d4, d1 + d3, d2, (double)0.0F, (double)0.0F, (double)0.0F);
         } else if (i == 2) {
            par1World.a("reddust", d + d4, d1 + d3, d2, (double)0.0F, (double)0.0F, (double)0.0F);
         } else if (i == 3) {
            par1World.a("reddust", d, d1 + d3, d2 - d4, (double)0.0F, (double)0.0F, (double)0.0F);
         } else if (i == 4) {
            par1World.a("reddust", d, d1 + d3, d2 + d4, (double)0.0F, (double)0.0F, (double)0.0F);
         } else {
            par1World.a("reddust", d, d1, d2, (double)0.0F, (double)0.0F, (double)0.0F);
         }

      }
   }
}
