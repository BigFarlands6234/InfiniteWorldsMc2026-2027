import java.util.ArrayList;
import java.util.List;

public class wu {
   private static List g = new ArrayList();
   private static int h = 0;
   public double a;
   public double b;
   public double c;
   public double d;
   public double e;
   public double f;

   public static wu a(double par0, double par2, double par4, double par6, double par8, double par10) {
      return new wu(par0, par2, par4, par6, par8, par10);
   }

   public static void a() {
      g.clear();
      h = 0;
   }

   public static void b() {
      h = 0;
   }

   public static wu b(double par0, double par2, double par4, double par6, double par8, double par10) {
      if (h >= g.size()) {
         g.add(a((double)0.0F, (double)0.0F, (double)0.0F, (double)0.0F, (double)0.0F, (double)0.0F));
      }

      return ((wu)g.get(h++)).c(par0, par2, par4, par6, par8, par10);
   }

   private wu(double par1, double par3, double par5, double par7, double par9, double par11) {
      this.a = par1;
      this.b = par3;
      this.c = par5;
      this.d = par7;
      this.e = par9;
      this.f = par11;
   }

   public wu c(double par1, double par3, double par5, double par7, double par9, double par11) {
      this.a = par1;
      this.b = par3;
      this.c = par5;
      this.d = par7;
      this.e = par9;
      this.f = par11;
      return this;
   }

   public wu a(double par1, double par3, double par5) {
      double d = this.a;
      double d1 = this.b;
      double d2 = this.c;
      double d3 = this.d;
      double d4 = this.e;
      double d5 = this.f;
      if (par1 < (double)0.0F) {
         d += par1;
      }

      if (par1 > (double)0.0F) {
         d3 += par1;
      }

      if (par3 < (double)0.0F) {
         d1 += par3;
      }

      if (par3 > (double)0.0F) {
         d4 += par3;
      }

      if (par5 < (double)0.0F) {
         d2 += par5;
      }

      if (par5 > (double)0.0F) {
         d5 += par5;
      }

      return b(d, d1, d2, d3, d4, d5);
   }

   public wu b(double par1, double par3, double par5) {
      double d = this.a - par1;
      double d1 = this.b - par3;
      double d2 = this.c - par5;
      double d3 = this.d + par1;
      double d4 = this.e + par3;
      double d5 = this.f + par5;
      return b(d, d1, d2, d3, d4, d5);
   }

   public wu c(double par1, double par3, double par5) {
      return b(this.a + par1, this.b + par3, this.c + par5, this.d + par1, this.e + par3, this.f + par5);
   }

   public double a(wu par1AxisAlignedBB, double par2) {
      if (!(par1AxisAlignedBB.e <= this.b) && !(par1AxisAlignedBB.b >= this.e)) {
         if (!(par1AxisAlignedBB.f <= this.c) && !(par1AxisAlignedBB.c >= this.f)) {
            if (par2 > (double)0.0F && par1AxisAlignedBB.d <= this.a) {
               double d = this.a - par1AxisAlignedBB.d;
               if (d < par2) {
                  par2 = d;
               }
            }

            if (par2 < (double)0.0F && par1AxisAlignedBB.a >= this.d) {
               double d1 = this.d - par1AxisAlignedBB.a;
               if (d1 > par2) {
                  par2 = d1;
               }
            }

            return par2;
         } else {
            return par2;
         }
      } else {
         return par2;
      }
   }

   public double b(wu par1AxisAlignedBB, double par2) {
      if (!(par1AxisAlignedBB.d <= this.a) && !(par1AxisAlignedBB.a >= this.d)) {
         if (!(par1AxisAlignedBB.f <= this.c) && !(par1AxisAlignedBB.c >= this.f)) {
            if (par2 > (double)0.0F && par1AxisAlignedBB.e <= this.b) {
               double d = this.b - par1AxisAlignedBB.e;
               if (d < par2) {
                  par2 = d;
               }
            }

            if (par2 < (double)0.0F && par1AxisAlignedBB.b >= this.e) {
               double d1 = this.e - par1AxisAlignedBB.b;
               if (d1 > par2) {
                  par2 = d1;
               }
            }

            return par2;
         } else {
            return par2;
         }
      } else {
         return par2;
      }
   }

   public double c(wu par1AxisAlignedBB, double par2) {
      if (!(par1AxisAlignedBB.d <= this.a) && !(par1AxisAlignedBB.a >= this.d)) {
         if (!(par1AxisAlignedBB.e <= this.b) && !(par1AxisAlignedBB.b >= this.e)) {
            if (par2 > (double)0.0F && par1AxisAlignedBB.f <= this.c) {
               double d = this.c - par1AxisAlignedBB.f;
               if (d < par2) {
                  par2 = d;
               }
            }

            if (par2 < (double)0.0F && par1AxisAlignedBB.c >= this.f) {
               double d1 = this.f - par1AxisAlignedBB.c;
               if (d1 > par2) {
                  par2 = d1;
               }
            }

            return par2;
         } else {
            return par2;
         }
      } else {
         return par2;
      }
   }

   public boolean a(wu par1AxisAlignedBB) {
      if (!(par1AxisAlignedBB.d <= this.a) && !(par1AxisAlignedBB.a >= this.d)) {
         if (!(par1AxisAlignedBB.e <= this.b) && !(par1AxisAlignedBB.b >= this.e)) {
            return par1AxisAlignedBB.f > this.c && par1AxisAlignedBB.c < this.f;
         } else {
            return false;
         }
      } else {
         return false;
      }
   }

   public wu d(double par1, double par3, double par5) {
      this.a += par1;
      this.b += par3;
      this.c += par5;
      this.d += par1;
      this.e += par3;
      this.f += par5;
      return this;
   }

   public boolean a(bo par1Vec3D) {
      if (!(par1Vec3D.a <= this.a) && !(par1Vec3D.a >= this.d)) {
         if (!(par1Vec3D.b <= this.b) && !(par1Vec3D.b >= this.e)) {
            return par1Vec3D.c > this.c && par1Vec3D.c < this.f;
         } else {
            return false;
         }
      } else {
         return false;
      }
   }

   public double c() {
      double d = this.d - this.a;
      double d1 = this.e - this.b;
      double d2 = this.f - this.c;
      return (d + d1 + d2) / (double)3.0F;
   }

   public wu e(double par1, double par3, double par5) {
      double d = this.a + par1;
      double d1 = this.b + par3;
      double d2 = this.c + par5;
      double d3 = this.d - par1;
      double d4 = this.e - par3;
      double d5 = this.f - par5;
      return b(d, d1, d2, d3, d4, d5);
   }

   public wu d() {
      return b(this.a, this.b, this.c, this.d, this.e, this.f);
   }

   public pl a(bo par1Vec3D, bo par2Vec3D) {
      bo vec3d = par1Vec3D.a(par2Vec3D, this.a);
      bo vec3d1 = par1Vec3D.a(par2Vec3D, this.d);
      bo vec3d2 = par1Vec3D.b(par2Vec3D, this.b);
      bo vec3d3 = par1Vec3D.b(par2Vec3D, this.e);
      bo vec3d4 = par1Vec3D.c(par2Vec3D, this.c);
      bo vec3d5 = par1Vec3D.c(par2Vec3D, this.f);
      if (!this.b(vec3d)) {
         vec3d = null;
      }

      if (!this.b(vec3d1)) {
         vec3d1 = null;
      }

      if (!this.c(vec3d2)) {
         vec3d2 = null;
      }

      if (!this.c(vec3d3)) {
         vec3d3 = null;
      }

      if (!this.d(vec3d4)) {
         vec3d4 = null;
      }

      if (!this.d(vec3d5)) {
         vec3d5 = null;
      }

      bo vec3d6 = null;
      if (vec3d != null && (vec3d6 == null || par1Vec3D.e(vec3d) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d;
      }

      if (vec3d1 != null && (vec3d6 == null || par1Vec3D.e(vec3d1) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d1;
      }

      if (vec3d2 != null && (vec3d6 == null || par1Vec3D.e(vec3d2) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d2;
      }

      if (vec3d3 != null && (vec3d6 == null || par1Vec3D.e(vec3d3) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d3;
      }

      if (vec3d4 != null && (vec3d6 == null || par1Vec3D.e(vec3d4) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d4;
      }

      if (vec3d5 != null && (vec3d6 == null || par1Vec3D.e(vec3d5) < par1Vec3D.e(vec3d6))) {
         vec3d6 = vec3d5;
      }

      if (vec3d6 == null) {
         return null;
      } else {
         byte byte0 = -1;
         if (vec3d6 == vec3d) {
            byte0 = 4;
         }

         if (vec3d6 == vec3d1) {
            byte0 = 5;
         }

         if (vec3d6 == vec3d2) {
            byte0 = 0;
         }

         if (vec3d6 == vec3d3) {
            byte0 = 1;
         }

         if (vec3d6 == vec3d4) {
            byte0 = 2;
         }

         if (vec3d6 == vec3d5) {
            byte0 = 3;
         }

         return new pl(0L, 0, 0L, byte0, vec3d6);
      }
   }

   private boolean b(bo par1Vec3D) {
      if (par1Vec3D == null) {
         return false;
      } else {
         return par1Vec3D.b >= this.b && par1Vec3D.b <= this.e && par1Vec3D.c >= this.c && par1Vec3D.c <= this.f;
      }
   }

   private boolean c(bo par1Vec3D) {
      if (par1Vec3D == null) {
         return false;
      } else {
         return par1Vec3D.a >= this.a && par1Vec3D.a <= this.d && par1Vec3D.c >= this.c && par1Vec3D.c <= this.f;
      }
   }

   private boolean d(bo par1Vec3D) {
      if (par1Vec3D == null) {
         return false;
      } else {
         return par1Vec3D.a >= this.a && par1Vec3D.a <= this.d && par1Vec3D.b >= this.b && par1Vec3D.b <= this.e;
      }
   }

   public void b(wu par1AxisAlignedBB) {
      this.a = par1AxisAlignedBB.a;
      this.b = par1AxisAlignedBB.b;
      this.c = par1AxisAlignedBB.c;
      this.d = par1AxisAlignedBB.d;
      this.e = par1AxisAlignedBB.e;
      this.f = par1AxisAlignedBB.f;
   }

   public String toString() {
      return "box[" + this.a + ", " + this.b + ", " + this.c + " -> " + this.d + ", " + this.e + ", " + this.f + "]";
   }
}
