package net.minecraft;

class Util$7 extends Thread {
   Util$7(String arg0) {
      super(arg0);
   }

   public void run() {
      while(true) {
         try {
            Thread.sleep(2147483647L);
         } catch (InterruptedException var2) {
            Util.LOGGER.warn("Timer hack thread interrupted, that really should not happen");
            return;
         }
      }
   }
}
