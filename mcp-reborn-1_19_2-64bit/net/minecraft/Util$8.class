package net.minecraft;

import com.google.common.collect.Maps;
import java.util.Map;
import java.util.function.Function;

class Util$8 implements Function<T, R> {
   private final Map<T, R> cache;

   Util$8(Function var1) {
      this.val$p_143828_ = var1;
      this.cache = Maps.newHashMap();
   }

   public R apply(T p_214691_) {
      return (R)this.cache.computeIfAbsent(p_214691_, this.val$p_143828_);
   }

   public String toString() {
      Function var10000 = this.val$p_143828_;
      return "memoize/1[function=" + var10000 + ", size=" + this.cache.size() + "]";
   }
}
